eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/

server:
  port: 8020
spring:
  application:
    name: service-hzconfig
  cloud:
    config:
      server:
        native:
          search-locations: /Users/bin/work/hzcp/config
  profiles:
    active: native

